use specs::{World, WorldExt};

use crate::ExtraLayers;

pub fn load_layers(
    nrows: u32,
    ncols: u32,
    layers: &[tiled::Layer],
    world: &mut World,
    extra_layers: &mut ExtraLayers,
) {
    todo!()
}
